import { View, StyleSheet } from 'react-native';
import Card from './Card';


export default function CardProfile ({
    _id,
    name,
    email,
    profileImage
}) {
    return (
        <Card>

        </Card>
    )
}